plugins {
    id 'application'
}

group rootProject.group + '.cli'
version '1.0-SNAPSHOT'

dependencies {
    implementation "com.github.ajalt:clikt:2.6.0"
    implementation project(":lib")
}

application {
    mainClassName "dev.stashy.ffmanager.ui.ProgramKt"
}